<BiRow>
<template #en>

To share a memory:

</template>
<template #zh>

共享记忆的步骤：

</template>
</BiRow>

<BiRow>
<template #en>

1. Navigate to the **Memory** page and open the intended memory.
2. Click **Configurations**.
3. Change **Permissions** from **Only me** to **Team**.
4. Click **Save** to apply your changes.

</template>
<template #zh>

1. 进入 **Memory**（记忆）页面，打开目标记忆。
2. 点击 **Configurations**（配置）。
3. 将 **Permissions**（权限）从 **Only me**（仅自己）改为 **Team**（团队）。
4. 点击 **Save**（保存）应用更改。

</template>
</BiRow>

<BiRow>
<template #en>

After this configuration is saved, team members can see the shared memory.

</template>
<template #zh>

保存该配置后，团队成员就能看到共享的记忆。

</template>
</BiRow>
