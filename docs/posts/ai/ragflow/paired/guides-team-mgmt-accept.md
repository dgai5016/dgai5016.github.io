<BiRow>
<template #en>

When another team owner invites you to join a team, you can process the invitation on the **Team** page.

</template>
<template #zh>

当其他团队所有者邀请你加入团队时，你可以在 **Team**（团队）页面上处理该邀请。

</template>
</BiRow>

<BiRow>
<template #en>

If the membership role is `invite`, the **Joined teams** list displays **Agree** and **Refuse** operations.

</template>
<template #zh>

如果成员角色为 `invite`，**Joined teams**（已加入的团队）列表会显示 **Agree**（同意）和 **Refuse**（拒绝）操作。

</template>
</BiRow>

<BiRow>
<template #en>

- Click **Agree** to accept the invitation. The system changes the relationship role from `invite` to `normal`.
- Click **Refuse** to decline the invitation. The system deletes the invitation relationship.

</template>
<template #zh>

- 点击 **Agree** 接受邀请。系统会把关系角色从 `invite` 变更为 `normal`。
- 点击 **Refuse** 拒绝邀请。系统会删除该邀请关系。

</template>
</BiRow>

<BiRow>
<template #en>

Only an invitation relationship can be accepted. After accepting an invitation, you can access team-shared resources in that workspace when those resources also satisfy their sharing scope and operation permission rules.

</template>
<template #zh>

只有邀请关系才能接受。接受邀请后，当这些资源同时满足其共享范围和操作权限规则时，你才能访问该工作区中团队共享的资源。

</template>
</BiRow>

<BiRow>
<template #en>

![Accept Or Decline An Invitation](/ragflow-images/accept_or_decline_an_invitation_1.jpg)

</template>
<template #zh>

![接受或拒绝邀请](/ragflow-images/accept_or_decline_an_invitation_1.jpg)

</template>
</BiRow>

<BiRow>
<template #en>

![Accept Or Decline An Invitation](/ragflow-images/accept_or_decline_an_invitation_2.jpg)

</template>
<template #zh>

![接受或拒绝邀请](/ragflow-images/accept_or_decline_an_invitation_2.jpg)

</template>
</BiRow>
