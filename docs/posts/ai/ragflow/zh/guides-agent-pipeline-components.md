# 了解摄取管道核心组件

摄取管道由以下核心组件组成：

- **解析器（Parser）组件**：读取并理解你的文件，如 PDF、图片、邮件等其他文件类型，提取文本与结构。
- **转换器（Transformer）组件**：利用 AI 为文本添加摘要、关键词或问题，增强文本，提升搜索效果。
- **分块器（Chunker）组件**：把长文本切分为大小最优的片段，即分块（chunk），提升 AI 检索效果。
- **索引器（Indexer）组件**：最后一步。将处理后的数据发送到文档引擎，支持全文与向量混合搜索。
