# 共享 Agent

共享 Agent 的步骤：

1. 在编辑画布中打开目标 Agent。
2. 点击 **Management**（管理）**>** **Settings**（设置），打开 **Agent settings**（Agent 设置）对话框。
3. 将 **Permissions**（权限）从 **Only me**（仅自己）改为 **Team**（团队）。
4. 点击 **Save**（保存）应用更改。

保存该配置后，团队成员就能看到共享的 Agent。
